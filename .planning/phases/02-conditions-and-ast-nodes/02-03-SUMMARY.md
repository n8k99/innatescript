---
phase: 02-conditions-and-ast-nodes
plan: 03
subsystem: testing
tags: [common-lisp, sbcl, defstruct, ast, types, node-kinds, resistance, innate-result]

# Dependency graph
requires:
  - phase: 02-conditions-and-ast-nodes
    plan: 01
    provides: Package export contracts for innate.types (32 symbols) and test package innate.tests.types
  - phase: 02-conditions-and-ast-nodes
    plan: 02
    provides: conditions.lisp implementation (parallel wave 2 — not a blocker for types)
provides:
  - src/types.lisp — 20 defconstant +node-*+ keyword constants, defstruct node (4 slots), defstruct innate-result (2 slots), defstruct resistance (2 typed string slots)
  - tests/test-types.lisp — 13 round-trip tests covering node kind constants, make-node slot access, defaults, kind/constant dispatch, innate-result, and resistance predicate
affects:
  - 03-parser (imports innate.types via :import-from; uses make-node and +node-*+ constants)
  - 05-evaluator (dispatches on node-kind using etypecase; returns innate-result and resistance values)
  - 07-evaluator-full (two-pass hoisting uses make-node :kind +node-program+)
  - 08-fulfillment (checks resistance-p on evaluator return values)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Universal node struct: single defstruct node with 4 slots dispatched via etypecase on node-kind — adding a node kind requires only a new defconstant, zero struct changes"
    - "defconstant +node-name+ :keyword — keyword self-evaluation makes this safe across SBCL image reloads"
    - "resistance struct (data value) distinct from innate-resistance condition (signal) — same conceptual role, different Lisp types; resistance-p predicate auto-generated by defstruct"

key-files:
  created:
    - tests/test-types.lisp
  modified:
    - src/types.lisp

key-decisions:
  - "Universal node defstruct (not defclass hierarchy) — etypecase dispatch on node-kind keyword keeps AST extensible without class redefinition overhead"
  - "defconstant for node kind constants is safe with keyword values — keywords are self-evaluating in SBCL, no redefinition error on image reload"
  - "resistance struct is a data return value; innate-resistance is a signalable condition — naming difference (resistance vs innate-resistance) prevents accessor collision"

patterns-established:
  - "AST node round-trip test: make-node + read accessor matches initarg value — establishes baseline correctness for parser output"
  - "defstruct predicate test (resistance-p t/nil) — both positive and negative cases, important because defstruct predicates are type-based"

requirements-completed: [PAR-22]

# Metrics
duration: 2min
completed: 2026-03-28
---

# Phase 02 Plan 03: AST Node Types and Round-Trip Tests Summary

**Single universal `defstruct node` with 20 keyword constants, innate-result and resistance structs fully implemented; 13 round-trip tests pass with 23/23 total test suite**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-28T19:14:51Z
- **Completed:** 2026-03-28T19:16:36Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- src/types.lisp fully implemented: 20 `defconstant +node-*+` constants covering all Innate AST node kinds, `defstruct node` with 4 slots (kind/value/children/props), `defstruct innate-result` with 2 slots (value/context), `defstruct resistance` with 2 typed string slots (message/source)
- tests/test-types.lisp created with 13 tests covering all public interfaces: node kind constants, make-node constructor with all 4 slots, default slot values, kind/constant EQL dispatch, innate-result slots, and resistance predicate (both true and false cases)
- Full test suite passes: 23/23 tests (4 smoke + 6 conditions + 13 types)
- SBCL compiler note about unreachable code in resistance-p test is expected and correct behavior (type-based predicate always returns NIL for strings)

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement defstructs and node kind constants in src/types.lisp** - `8595276` (feat)
2. **Task 2: Write round-trip tests in tests/test-types.lisp** - `a601380` (feat)

## Files Created/Modified

- `src/types.lisp` — Replaced stub with 20 defconstant forms, defstruct node (4 slots), defstruct innate-result (2 slots), defstruct resistance (2 typed string slots)
- `tests/test-types.lisp` — Created with 13 deftest forms using (in-package :innate.tests.types)

## Decisions Made

- Used `defstruct` (not `defclass`) for AST node — `etypecase` dispatch on `(node-kind n)` keyword is the established pattern; adding a node kind requires only a new `defconstant`, zero struct changes
- `defconstant` with keyword values is safe on SBCL image reload because keywords are self-evaluating — no "attempting to redefine" error
- Wrote 13 tests (plan said 12 but `<done>` section described 13 behaviors: "resistance predicate and slot (3)"); plan acceptance criteria had off-by-one vs behavior list

## Deviations from Plan

None - plan executed exactly as written. The test count (13 vs 12) is a clarification: the plan's `<done>` description says "resistance predicate and slot (3)" which implies 3 resistance tests, matching the behavior list. All specified behaviors are covered.

## Issues Encountered

None. SBCL "note: deleting unreachable code" for `test-resistance-predicate-false` is expected — the compiler knows statically that `(resistance-p "string")` is always NIL. Not a warning, not an error.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All downstream phases can import innate.types symbols — make-node, +node-*+ constants, make-innate-result, make-resistance, resistance-p are all implemented and tested
- Phase 03 (parser/tokenizer) can proceed: node constructors and kind constants are ready
- Phase 05 (evaluator) can proceed: innate-result and resistance return types are ready
- Wave 2 of Phase 02 is complete: both types.lisp and conditions.lisp implementations are done

---
*Phase: 02-conditions-and-ast-nodes*
*Completed: 2026-03-28*
